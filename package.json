{
  "name": "udacity-maps-submission",
  "version": "1.0.0",
  "description": "Neighborhood map submission",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server -a localhost"
  },
  "author": "Henry Ollarves",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4",
    "http-server": "^0.10.0"
  }
}
